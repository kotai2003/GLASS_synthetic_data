# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository layout

This workspace wraps an upstream research codebase **and** builds a GUI application on top of its anomaly-synthesis branch:

- `GLASS/` — upstream PyTorch implementation (its own git repo, MIT-licensed, mirror of `cqylunlun/GLASS`, gitignored from the parent repo). Treat as the working root for any model/training change.
- `GLASS/dump_synthetic.py` — local CLI helper (added by this workspace, not upstream). Since 2026-05-02 it is a thin wrapper that delegates to `synthesize_gui.core.synthesis.synthesize_one`; output layout (`original/synthetic/mask/panel`) is unchanged for backward compat. The GUI app package now lives one level up at `../synthesize_gui/` (sibling of `GLASS/`), so this script adds its parent dir to `sys.path` before importing. **Caveat:** this `sys.path` + import edit is tracked only in the *inner* `GLASS` repo, and as of 2026-05-17 it is committed there but **not pushable** (inner `origin` = upstream `cqylunlun/GLASS`, push-forbidden). Don't `git checkout`/`reset` inside `GLASS/` without preserving it, or `dump_synthetic.py` will revert to importing the now-absent `synthesizer_app`.
- `synthesize_gui/` — **NG-data synthesis GUI app**, the long-term deliverable. Moved out of the GLASS inner repo on 2026-05-17; it now sits at `01.GLASS/synthesize_gui/` (sibling of `GLASS/`) and is tracked by the **parent `01.GLASS` git repo** (not the GLASS inner repo, not via subtree split anymore — see "Standalone publication" below).
  - `core/synthesis.py` — pure LAS function `synthesize_one(image, texture, params, fg_mask=None) -> SynthResult`. Single source of truth for Perlin × DTD blending in this workspace.
  - `core/_vendored/perlin.py` — vendored copy of upstream `GLASS/perlin.py` (MIT, attribution preserved). `synthesis.py` imports from here, **not** from `GLASS/perlin.py`, so the app is clone-and-run self-contained.
  - `core/io_utils.py` — generic recursive image enumeration (so non-DTD texture sources can plug in later).
  - `core/exporter.py` — MVTec-compatible writer (`train/good`, `test/synthetic`, `ground_truth/synthetic`, `run.json`).
  - `ui/custom_styles_jp.py`, `ui/TR_inc_logo.png` — TOMOMI RESEARCH unified GUI style assets, copied from `skills/tomomi-gui-style/`. The logo is composited onto the ttk background at load time (the PNG has a baked-in black bg; see `gui_main_ui._build_control_tab`).
  - `ui/app_icon.ico`, `ui/app_icon.png` — window/taskbar icon (also the EXE icon). Generated by `ui/_make_icon.py` (asset utility, underscore-excluded from `unittest discover`; re-run `python -m synthesize_gui.ui._make_icon` after changing brand colors).
  - `ui/gui_main_ui.py`, `gui_main.py` — pure Tkinter + ttk app (no Pygubu, per user decision). Threaded batch worker + `queue.Queue`-based main-thread dispatch.
  - `tests/test_synthesis.py` — unittest for shape, determinism, fg-mask validation.
  - `tests/_capture_manual_screens.py` — doc utility (NOT a unit test). Launches the GUI with `-topmost`, drives tab switches and a Preview synthesis programmatically, and writes 7 PNGs to `00.docs/manual_screens/` (resolved as `<synthesize_gui parent>/00.docs`, i.e. `01.GLASS/00.docs`). Re-run after any UI change to refresh the manual's screenshots. Underscore prefix excludes it from `unittest discover`.
  - `LICENSE`, `LICENSE_GLASS`, `README.md`, `requirements.txt` — standalone-repo metadata.
- `GLASS/requirements_original.txt` — verbatim copy of `requirements.txt`. Used as the canonical install for the `GLASS` conda env.
- `GLASS/requirements_updated_current_env.txt` — abandoned attempt to install GLASS into the system's Python 3.13 env. Kept for history only; do not install from it.
- `00.docs/` — reference PDF of the GLASS paper (`2407.09359v1.pdf`, ECCV 2024), `GLASS_synth_gui_plan.md` (the GUI-app implementation plan with phase breakdown and confirmed decisions), `GLASS_synthesizer_user_manual.md` (Japanese end-user manual covering every widget and typical workflows, with embedded screenshots), and `manual_screens/*.png` (the 1600×900 captures referenced from that manual; regenerated by `synthesize_gui/tests/_capture_manual_screens.py`).
- `01.reports/GLASS_execution_plan.md` — detailed reproduction plan (datasets, risks, commands).
- `01.reports/GLASS_dependency_install_report.md` — record of how the `GLASS` conda env was actually built and verified, including the imgaug × NumPy 2.x detour.
- `synthetic_dump/<class>/{original,synthetic,mask,panel}/` — sample output of `dump_synthetic.py`. Inspectable evidence of the LAS branch's synthesis quality.
- `skills/` — local Claude skills (`karphathy-guidelines/`, `tomomi-gui-style/`). The GUI-style skill is the design system the synthesizer GUI must follow.
- **Build pipeline** (top level, all tracked; see "Building the protected `.exe` distributable"): `build_all.py` (orchestrator), `setup_cython.py` (cythonize), `glass_app.py` (thin launcher — only readable first-party source in the bundle), `glass_synthesizer.spec` (PyInstaller ONEDIR), `glass_synthesizer_setup.iss` (Inno Setup installer script). Build outputs go to `C:\TR_build\GLASS\` (outside OneDrive); only `build_all.log` returns to the repo.

## Environment

Use the existing **`GLASS` conda environment** for anything GLASS-related. Don't try to reuse the system `synthetic_data_py313` (Python 3.13 + numpy 2.x); the upstream `imgaug==0.4.0` calls `np.sctypes` which was removed in NumPy 2.0, and there are no Python 3.13 wheels for `numpy<2`.

The `GLASS` env was built to match the upstream pinning exactly:

- **conda env path**: `C:\Users\seong\anaconda3\envs\GLASS`
- **Python**: 3.9.15
- **PyTorch**: 2.1.2+cu118 / torchvision 0.16.2+cu118 (installed from `https://download.pytorch.org/whl/cu118` *before* `pip install -r requirements_original.txt`, otherwise pip auto-picks a CPU wheel)
- **CUDA wheel**: cu118; the local NVIDIA driver (595.97) supports CUDA 13 but is forward-compatible with cu118 binaries — verified working at runtime
- **GPU**: NVIDIA RTX 3060 Laptop, **6 GB VRAM** — the upstream `--batch_size 8` setting may OOM; smoke test ran with `--batch_size 2`
- **All other deps** at their upstream pins (numpy 1.26.3, imgaug 0.4.0, timm 0.9.12, pandas 1.5.2, etc.)

Conda is not on `PATH` in the default Git Bash session. Either invoke the env's interpreter directly:

```bash
GLASS_PY="C:/Users/seong/anaconda3/envs/GLASS/python.exe"
"$GLASS_PY" main.py ...
```

…or activate via the full conda path:

```bash
"C:/Users/seong/anaconda3/Scripts/conda.exe" activate GLASS
```

## Local datasets and quirks (this workstation)

These paths are pinned for the host the workspace lives on. Pass them via CLI; do **not** edit upstream `shell/run-*.sh` to bake them in (those edits would dirty the upstream working tree).

| Flag | Path | Notes |
|---|---|---|
| `--datapath` (mvtec) | `C:/Datasets-rev002/01.MVTEC_Anomaly_Detection` | All 15 standard classes present |
| `--augpath` | `C:/Datasets-rev002/DTD/images` | Full DTD r1.0.1: 47 categories × ~120 jpg = 5640 jpg |

Dataset quirks already accommodated:

- **Class name mismatch**: the local layout has `leather_original` instead of `leather`. A Windows directory junction `C:\Datasets-rev002\01.MVTEC_Anomaly_Detection\leather` → `leather_original` was created so GLASS's hardcoded class name resolves correctly. Don't rename the junction or the underlying folder.
- **No `fg_mask/`**: the official Foreground Mask zip from the README's Google Drive has not been downloaded. **Run with `--fg 0`** until it is. `--fg 1` will crash inside `MVTecDataset.__getitem__` because of the missing `fg_mask/<class>/<file>.png` lookup.
- **`leather`/`carpet` shells contain extra sibling folders** (`capsule_wo_bg_clean`, `transistor-original`, `carpet/test-backup`, etc.). Harmless — GLASS only iterates folders matching its hardcoded class list. Don't include them in `-d` flags.

## Reproduction state

End-to-end smoke test on `mvtec_carpet` succeeded (2026-05-01):

- Command: `--meta_epochs 1 --limit 8 --batch_size 2 --distribution 2 --fg 0 -d carpet`
- Runtime: ~11 min (≈8 min was first-iteration CUDA warmup; subsequent iterations ≈1 sec each)
- Result: I-AUROC 92.42 / P-AUROC 93.90 / P-PRO 78.03 with only 10 training samples seen (well below the paper's 99.9 / 99.3 but the pipeline runs cleanly)
- Artifacts produced: `GLASS/results/models/backbone_0/mvtec_carpet/{ckpt.pth, ckpt_best_0.pth, tb/}`, 117 PNGs each in `results/{training,eval}/mvtec_carpet/`, `results/results.csv`

What this means for future runs:

- Full upstream training (`--meta_epochs 640 --limit 392 --batch_size 8` × 15 classes) is not feasible on the 6 GB GPU within reasonable wall time. Either drop to a much smaller `meta_epochs`/`limit`, swap to a smaller backbone, or use the upstream-released checkpoints for evaluation (`--test test`).
- For "does the synthesis look right" investigations, prefer `dump_synthetic.py` (no GPU at all) over running training.
- `--distribution 2` (force manifold) is the cleanest setting for isolated runs because it skips the xlsx side-effect machinery; only switch back to `--distribution 0/1` when you specifically want the spectrogram judgment.

`onnx`, `onnxruntime-gpu`, and `onnxsim` are installed (per upstream pins) but the export workflow has not been exercised on this workstation. `onnxruntime-gpu==1.18.1` is built for CUDA 11.8 + cuDNN 8.x; if it fails at runtime under the current driver, fall back to CPU `onnxruntime`.

## Running training and evaluation

All entry points go through `GLASS/main.py`, a click chain of three subcommands invoked in order: top-level options → `net ...` → `dataset ...`. Driver scripts in `GLASS/shell/run-*.sh` show the canonical argument set per dataset (`run-mvtec.sh`, `run-visa.sh`, `run-mpdd.sh`, `run-wfdd.sh`, `run-mad-man.sh`, `run-mad-sys.sh`, `run-custom.sh`).

Important conventions baked into those shell scripts:

- They `cd ..` first so `python main.py` runs from the `GLASS/` root, not from `shell/`. Output paths like `./results/...` and `./datasets/excel/...` are resolved relative to `GLASS/`.
- Top-level `--test ckpt` = train + evaluate; `--test test` = evaluation only (loads `ckpt_best_*.pth` from the model dir).
- `--datapath` and `--augpath` must exist (click validates with `exists=True`). `augpath` is the DTD texture dataset used to synthesize anomalies.
- `-d` repeats per subdataset (class). The shell scripts build `flags=()` from a `classes=(...)` array.
- For a new dataset, copy `run-custom.sh` and adapt; `main.py` only knows four dataset registries (`mvtec`, `visa`, `mpdd`, `wfdd`), and `mpdd`/`wfdd` reuse `MVTecDataset`. A "custom" dataset must follow MVTec's directory layout (`<class>/{train,test,ground_truth}/...`).

To run a single class on MVTec for debugging, edit the `classes=(...)` array in the shell script (or invoke `python main.py ... -d <class> mvtec <datapath> <augpath>` directly).

## Distribution / foreground flags (non-obvious)

These two args control GLASS's two modeling choices and have meanings beyond plain bool:

`--distribution`:
- `0` — read SVD/manifold choice from `./datasets/excel/<dataset>_distribution.xlsx` (the file `main.py` writes after a spectrogram-judgment pass).
- `1` — judge per-class by image-level spectrogram (`utils.distribution_judge`); training short-circuits and writes the xlsx row instead of training.
- `2` — force manifold (`svd=0`).
- `3` — force hypersphere (`svd=1`).
- `4` — read the xlsx but invert the choice.

`--fg`:
- `0` — no foreground mask.
- `1` — load `<datapath>/fg_mask/<class>/<file>.png` for every train image. Required by Perlin-mask synthesis when used.
- `2` — read per-class foreground decision from the xlsx.

If `--fg 1`, the foreground mask folder must exist alongside the class folders or training will crash inside `MVTecDataset.__getitem__`.

The first run on a new dataset is typically `--distribution 1 --test ckpt` to populate the xlsx; subsequent runs use `--distribution 0`.

## Pretrained results

The README documents a Google Drive `results/` folder with `ckpt_best_*.pth` weights for MVTec. Drop it at `GLASS/results/` before running with `--test test`. `glass.GLASS.tester` looks up `ckpt_best*` via glob in `<results_path>/models/backbone_<i>/<dataset>_<class>/`.

## ONNX export

`GLASS/onnx/pth2onnx.py` exports a single trained checkpoint to ONNX, then runs `onnxsim.simplify`. The script has hardcoded paths (`/root/.cache/torch/hub/...` for backbone weights, `../results/models/backbone_0/wfdd_grid_cloth/...` for the checkpoint) — edit them before running. `onnx/ort.py` is a standalone CUDA inference smoke test against the exported model.

## Code architecture

The training pipeline is built around three composed pieces:

1. **Backbone feature extractor** (`backbones.py` + `common.NetworkFeatureAggregator`). `backbones.load(name)` returns a torchvision/timm pretrained model. Features are pulled via forward hooks on named layers (`-le layer2 -le layer3` for the wide_resnet50_2 default), and the last-requested layer raises `LastLayerToExtractReachedException` to short-circuit the rest of the forward pass. Patches are extracted by `model.PatchMaker` (Unfold with `patchsize=3`, `stride=1`), mean-pooled to `pretrain_embed_dimension`, and aggregated to `target_embed_dimension`.

2. **Anomaly synthesis** (`datasets/mvtec.py` + `perlin.py`). For each training sample, a random DTD texture is loaded, a Perlin-noise mask is generated and (optionally) ANDed with the foreground mask, and the augmented image is `image * (1 - mask) + (1 - β) * aug * mask + β * image * mask` with `β ~ N(mean, std)` clipped to `[0.2, 0.8]`. `mask_s` (downsampled, feature-resolution) is the per-patch label fed to the discriminator.

3. **Discriminator + projection + gradient-ascent mining** (`glass.py` + `model.py`). `Projection` is an MLP applied to feature patches; `Discriminator` is a small MLP+sigmoid that scores each patch as anomalous. Per training step (`_train_discriminator`):
   - True features and Gaussian-perturbed copies are scored; BCE loss trains the discriminator to separate them.
   - Inner loop (`--step`, default 20) does gradient ascent on `gaus_feats` w.r.t. `gaus_loss` to mine harder negatives, projecting them back onto an annulus around the class center `c` (svd=1, hypersphere) or around the true features (svd=0, manifold) every 5 steps. `--radius` controls the projection quantile.
   - Synthesized fake features (from the Perlin-masked augmentations) get a focal loss against `mask_s`; with `--p > 0`, only the hardest top-p quantile contributes.
   - One epoch is capped at `--limit` samples (default 392) regardless of dataset size.

`GLASS.trainer` runs `--meta_epochs` of this, evaluates every `--eval_epochs` epoch on the val/test loader, and keeps the checkpoint with the best `image_auroc + pixel_auroc`. `GLASS.tester` reloads the best checkpoint and recomputes metrics including PRO (`metrics.compute_pro`, ~minutes per class).

## Outputs and side effects to be aware of

A run scribbles into several `GLASS/`-relative paths:
- `./results/models/backbone_<i>/<dataset>_<class>/ckpt_best_<epoch>.pth` — best checkpoint (only one kept; older ones are deleted in-place).
- `./results/models/.../ckpt.pth` — latest (overwritten every epoch).
- `./results/models/.../tb/` — TensorBoard logs.
- `./results/training/<dataset>_<class>/` — image / GT / heatmap triptychs from the most recent eval.
- `./results/eval/<dataset>_<class>/` — copy of `training/` snapshotted when a new best was set.
- `./results/judge/{avg,fft}/<flag>/<name>.png` — spectrogram-judgment artifacts (only when `--distribution 1`).
- `./datasets/excel/<dataset>_distribution.xlsx` — distribution/foreground decisions, created and read by both `main.py` and the dataset class. `MVTecDataset` reads it during `__init__` if `--fg 2`.
- `./results/results.csv` — aggregate metrics, rewritten after each class.

`utils.create_storage_folder` ignores `log_project`/`log_group`/`run_name` despite their being CLI options — `results_path` is used verbatim. Don't rely on the iteration/overwrite mode argument either.

## Skills in this workspace

`skills/karphathy-guidelines/SKILL.md` and `skills/tomomi-gui-style/SKILL.md` are local skills available to Claude Code in this directory. **The GUI-style skill is now load-bearing** — `synthesize_gui/ui/` must follow it (Meiryo 12 / `primary.TButton` / `custom.TLabelframe` / TR copyright footer / threaded workers + `root.after(0, ...)` posting).

## NG-data synthesis GUI app (work in progress)

The end deliverable of this workspace is **a Tkinter desktop app that synthesizes NG (anomaly) images + binary masks for downstream AI anomaly-detection training**. The GLASS reproduction work was groundwork for this; the app reuses GLASS's LAS branch only (GAS / discriminator / training loop are out of scope).

Confirmed decisions (see `00.docs/GLASS_synth_gui_plan.md` §10):

| Decision | Value |
|---|---|
| GUI framework | pure Tkinter + ttk (no Pygubu) |
| Output format | MVTec-compatible only (`<class>/test/synthetic/`, `<class>/ground_truth/synthetic/`) |
| OK-image resolution | preserve original; blend runs at output res, `working_size` only sets Perlin-mask granularity (see "Synthesis API" — 2026-05-19 full-res-blend rework) |
| Texture source | extensible: any directory walked recursively for image files (DTD today, in-house defect photos later) |
| Class scope | one class per app instance |
| Foreground mask absent | warn but proceed |
| `dump_synthetic.py` | refactored to call `synthesize_gui.core.synthesis` (regression-checked) |

Phase status: **Phases 0–5 complete (2026-05-02): core synthesis + CLI refactor + unit tests, full GUI (Control/Configure tabs, threaded worker, thumbnail strip).** As of 2026-05-18 the app is also **GPU-enabled** (`SynthParams.device`), **branded** (logo bg fix + custom app icon), and **packaged**: a Cython-protected PyInstaller ONEDIR build plus an Inno Setup installer (see "Building the protected `.exe` distributable"). **2026-05-19: synthesis reworked to a full-resolution, un-normalized, soft-mask blend** — only the Perlin mask is generated at `working_size`; non-anomalous pixels are now bit-identical to the source (fixes the whole-image texture-degradation reported by the user; see "Synthesis API").

End-user documentation lives at `00.docs/GLASS_synthesizer_user_manual.md` (Japanese; covers every widget on the Control / Configure tabs, the preview area, the thumbnail strip, and typical workflows, with screenshots from `00.docs/manual_screens/`). When you change widget behavior, layout, or labels, **also re-run `synthesize_gui/tests/_capture_manual_screens.py`** to refresh those PNGs — the script preset paths are pinned to this workstation's MVTec/DTD locations so it works out of the box here.

### Synthesis API (single source of truth)

```python
from synthesize_gui.core.synthesis import SynthParams, synthesize_one
import numpy as np, torch, imgaug

# All three RNGs must be seeded for reproducibility — perlin.py uses imgaug
# internally (iaa.Affine rotate), which has its own RNG.
np.random.seed(0); torch.manual_seed(0); imgaug.seed(0)

result = synthesize_one(
    image=PIL.Image.open("ok.png").convert("RGB"),
    texture=PIL.Image.open("dtd.jpg").convert("RGB"),
    params=SynthParams(working_size=288, output_size=None),  # None = preserve source res
)
# result.ng_image_bgr  : H x W x 3 BGR uint8 at output res
# result.mask_uint8    : H x W   0/255 uint8 binary mask at output res
# result.beta_used     : the sampled beta (for logging / labels.csv)
```

`working_size` must be divisible by `downsampling` (default 8) — Perlin mask generation is square-only upstream. **As of 2026-05-19 only the Perlin mask is generated at `working_size`**; the OK image and texture are kept at the output resolution and the beta-blend runs there (un-normalized), with the working-res mask bilinearly soft-upsampled to act as a smooth alpha. Consequence: every pixel the mask does not touch is **bit-identical to the source image** (no whole-image downscale→upscale and no ImageNet normalize roundtrip — the two former causes of whole-image "texture" degradation). `working_size` is now purely the anomaly-mask granularity / speed knob, not an image-quality ceiling. The determinism contract is unchanged (the `np.random.choice` texture-aug draw still happens first), so `tests/test_synthesis.py` (which asserts shape + same-seed determinism, not golden pixels) still passes; outputs are intentionally no longer pixel-equal to the pre-2026-05-19 working-size pipeline.

Run unit tests:

```bash
# from 01.GLASS/ (the folder that contains synthesize_gui/)
"$GLASS_PY" -m unittest synthesize_gui.tests.test_synthesis -v
```

### Building the protected `.exe` distributable (Cython + PyInstaller)

The shipped product is a **Cython-protected PyInstaller ONEDIR build** (never `--onefile`). This follows the TOMOMI standard established in `../../00.FORESIGHT_VIEWER_TR100` (thin `.py` launcher + `.pyd` logic + spec + `build_all.py` with post-build verification). Pipeline files live at `01.GLASS/` (all tracked; outputs gitignored):

| File | Role |
|---|---|
| `setup_cython.py` | `cythonize`s every `synthesize_gui` module except `__init__.py` |
| `glass_app.py` | Thin launcher — the **only** first-party readable source in the bundle. Has no IP; it explicitly imports every heavy dep + every protected submodule so PyInstaller's scanner bundles them (it cannot see inside a `.pyd`). |
| `glass_synthesizer.spec` | ONEDIR spec. Walks the **staged compiled** package, adds each `.pyd` as a binary at its package-relative path; `collect_all` for torch/torchvision/imgaug/skimage/scipy. |
| `build_all.py` | Orchestrator: Step1 stage+Cython → Step2 PyInstaller → Step3 verify. |

Build (≈ several min; output is ≈ 5–6 GB because the cu118 torch wheel bundles CUDA):

```bash
# from 01.GLASS/, with the GLASS env interpreter
"$GLASS_PY" build_all.py
```

**Build output goes OUTSIDE OneDrive** — default `C:\TR_build\GLASS\{build_cython_stage,build,dist}\` (override with the `GLASS_BUILD_ROOT` env var). The repo lives under a synced OneDrive folder; OneDrive grabs transient handles across a multi-GB output tree and intermittently breaks PyInstaller's `COLLECT` with `PermissionError: [WinError 5]`. Only `build_all.log` is written back into the repo. The distributable is therefore `C:\TR_build\GLASS\dist\GLASS_Synthesizer\GLASS_Synthesizer.exe`.

Non-obvious, load-bearing details:

- **Staging tree** (`…\build_cython_stage\`) — `build_all.py` copies `synthesize_gui/` there, compiles the 7 IP modules to `.pyd`, then **deletes the `.py` + generated `.c` in the stage**. The repo working tree is never modified, and no protected source can reach `dist/`. `build_all.py` passes the stage path to the spec via the `GLASS_STAGE_DIR` env var (and `--distpath`/`--workpath` for the out-of-OneDrive locations). Never point the spec/PyInstaller at the repo `synthesize_gui/` directly.
- **`__init__.py` stays as source on purpose** (all 4 are trivial import glue / re-exports, zero IP). They are the only `.py`/`.pyc` legitimately present in `dist/_internal/synthesize_gui/`. `core/__init__.py`'s `from .synthesis import …` resolves fine because `.pyd` outranks `.py` in Python's import order.
- The 7 protected modules: `core/{synthesis,exporter,io_utils}.py`, `core/_vendored/perlin.py`, `ui/{gui_main_ui,custom_styles_jp}.py`, `gui_main.py`. `tests/` and `ui/_make_icon.py` are excluded from the stage entirely.
- **Because the compiled `.pyd` modules are opaque to PyInstaller's import scanner, every dependency they import must be declared in `glass_app.py` (and/or spec `hiddenimports`).** This bit us twice and is the #1 gotcha for this pipeline:
  - `tkinter` / `tkinter.ttk` / `.filedialog` / `.messagebox` / `.font` — the UI `.pyd` import these; FORESIGHT never hit this because its `ui/` stayed as source.
  - `imageio` (via `imgaug`) calls `importlib.metadata.version("imageio")` at import → `PackageNotFoundError`. Fixed with `copy_metadata("imageio", recursive=True)` in the spec (plus imgaug/skimage/scipy/numpy/Pillow & their deps). Add `copy_metadata` for any future dep that does runtime metadata lookups.
- **Step 3 verification** fails the build if any protected `.py`/`.pyc`/`.c` appears under the bundled `synthesize_gui/` (scoped to that dir — third-party files like `torch/onnx/_internal/exporter.py` share basenames and must NOT trip it), plus a `.pyd` import smoke test. The launch check **inspects captured stderr for crash markers** (`Traceback`, `ModuleNotFoundError`, …) — it does **not** trust process liveness, because this is a `console=False` build and the PyInstaller error dialog keeps the crashed process alive (an "alive after Ns" check gives false PASS). Full pipeline verified PASS 2026-05-18 (GPU + imageio + tkinter fixes); re-verified 2026-05-19 from the out-of-OneDrive build root including the Inno Setup installer (silent install + installed-exe clean-launch); **rebuilt & re-verified PASS 2026-05-19 carrying the full-res-blend synthesis rework** (`synthesis.pyd` import smoke + EXE clean-launch OK; ISCC compile ~680 s → `installer\GLASS_Synthesizer_Setup_V.1.0.0.exe` 3.42 MB + `…-1.bin` ~1.78 GB).
- PyInstaller 6 **removed bytecode encryption** (`key=`/`cipher=`), which the older FORESIGHT spec used. Not a regression here: all real IP is native `.pyd`; only the trivial launcher/`__init__` glue remains as bytecode.
- `dump_synthetic.py` and `synthesize_gui.tests` still import the package as plain source from the repo (unaffected — this pipeline only touches the staged copy).

**Inno Setup installer (`build_all.py` Step 4 / `glass_synthesizer_setup.iss`):** After a green verification, Step 4 compiles the ONEDIR `dist/` into an installer via `ISCC.exe` (Inno Setup 6, found at `C:\Program Files (x86)\Inno Setup 6\ISCC.exe`; auto-located, **skipped cleanly if not installed** — the dist is still usable). Output: `C:\TR_build\GLASS\installer\`. The `.iss` is the trimmed TOMOMI standard (mirrors `00.FORESIGHT_VIEWER_TR100/iss/*.iss`) **without** FORESIGHT's `.lic`/file-association Pascal code (this app has no license mechanism). `SetupIconFile` and `LicenseFile` point at the **repo** (`synthesize_gui/ui/app_icon.ico`, `synthesize_gui/LICENSE`), which always exist regardless of build root.

Verified 2026-05-19: ISCC exit 0 (~690 s compile, `Compression=lzma2/normal` + `SolidCompression`). `DiskSpanning=yes` is set because the ~5.7 GB `dist/` exceeds Inno's 2 GB single-file limit — but LZMA2-solid compressed it to **~1.8 GB, which fit in a single span**, so the actual output is just `GLASS_Synthesizer_Setup_V.1.0.0.exe` (≈3.4 MB) **+ one** `GLASS_Synthesizer_Setup_V.1.0.0-1.bin` (≈1.78 GB). If the payload grows past a span, more `-N.bin` files appear. **Rule: ship `Setup.exe` and all `.bin` files together in the same folder** — the `.exe` alone is just the wizard stub, the payload is in the `.bin`(s). End-to-end checked: silent install (`/VERYSILENT /SUPPRESSMSGBOXES`) → exit 0, ~5.74 GB installed, and the **installed** exe launches clean (empty stderr, no traceback).

Install scope: the `.iss` leaves Inno's default `PrivilegesRequired=admin` → **per-machine** install to `{autopf}` (`Program Files`), writing an HKLM uninstall key. For a **no-admin per-user** installer, set `PrivilegesRequired=lowest` in the `.iss` (then `{autopf}`→`{localappdata}\Programs`) and recompile (~12 min). The session's silent-install verification ran elevated and its registry/Start-Menu artifacts were cleaned up afterward — a normal interactive install behaves the same (per-machine).

**GPU compute:** `SynthParams.device` (`"auto"` default → CUDA if available else CPU; `"cuda"` forces GPU and hard-errors if unavailable; `"cpu"` forces CPU). The beta-blend and image/texture/mask tensors run on the resolved device; output is bit-identical CPU vs GPU (verified). The vendored `perlin.py` is numpy/imgaug and **stays on CPU** (it requires a CPU `mask_fg` tensor and must not be modified — MIT vendored). So GPU only accelerates the blend, not the heavier Perlin/imgaug mask generation; net speedup on small tensors is modest. The GUI preview status shows `[GPU]`/`[CPU]`.

### Parent repo (`01.GLASS`) git workflow

The parent repo's `origin` is **https://github.com/kotai2003/GLASS_synthetic_data.git**, default branch `main`. The established workflow is **commit straight to `main` and `git push origin main`** (all history is linear on `main`; no PR flow, single-developer personal repo). `synthesize_gui/`, the top-level build pipeline, docs, and `00.docs/manual_screens/*.png` all live here. Build outputs (`build/`, `dist/`, `build_cython_stage/`, `*.pyd`, `*.c`, `*.log`) and `synthetic_dump/` are gitignored — only pipeline **sources** and `build_all.log` are tracked.

**CRITICAL — never stage the `GLASS` gitlink.** `GLASS/` appears in parent `git status` as a gitlink (`m GLASS` = dirty submodule content) because the inner repo carries the local `dump_synthetic.py` edit. When committing parent-repo work, **stage files explicitly** (or `git add` named paths) and leave `GLASS` unstaged — do **not** `git add GLASS` / `git add -A` it. Recording the gitlink pointer here is pointless (the inner `origin` is upstream `cqylunlun/GLASS`, push-forbidden) and risks clobbering that un-pushable local commit (see the top-of-file caveat). Verified 2026-05-21: commit `541bcdc` (synthesis rework + build pipeline + docs) was pushed this way with `GLASS` left untouched.

### Standalone publication (OUTDATED — needs revisiting)

**The previous subtree-split workflow is broken as of the 2026-05-17 move.** It relied on `synthesizer_app/` living *inside* the GLASS inner repo so `git subtree split --prefix=synthesizer_app` could carve it out. The app now lives at `01.GLASS/synthesize_gui/`, tracked by the **parent `01.GLASS` git repo**, and was moved with **no git history carried over** (deliberate, per the user's instruction). The `git subtree split --prefix=synthesizer_app` command no longer has anything to operate on.

The public repo https://github.com/kotai2003/glass-synthesizer-app (`syn_origin`, default branch `main`) still exists but is now stale. **Do not** attempt the old `git subtree split … / git push syn_origin syn_split:main` recipe — it will fail or push nothing. If/when standalone publication is wanted again, a new mechanism must be agreed with the user (e.g. a fresh subtree split with `--prefix=synthesize_gui` from the parent repo, or a plain mirror push). Until then, treat `synthesize_gui/` as a single-repo deliverable.

Still valid regardless of publication: the vendored `core/_vendored/perlin.py` is what makes `synthesize_gui/` runnable without GLASS — never replace it with a `from perlin import ...` that reaches into GLASS root, or a standalone copy will break on clone.
